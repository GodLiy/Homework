# 答辩分工讲解 — 6人 × 1分钟

**总时长**: 6 分钟讲解 + 2 分钟 Q&A = 8 分钟

---

## 成员1：系统部署（幻灯片3）— 1分钟

**讲什么**：我们做了什么部署，架构长什么样

```
我们选择了 SockShop 微服务系统，包含 14 个服务。
采用双重部署模式：
  Docker Compose 用于日常运行，访问 localhost:8088；
  Kubernetes/Minikube 用于 ChaosMesh 故障注入。
国内 Docker Hub 被墙，通过 Windows 系统代理拉取镜像。
整个系统 14 个容器一键启动，3 分钟内全部就绪。
```

**PPT页**: 幻灯片3（部署架构图）

---

## 成员2：监控 + ChaosMesh（幻灯片4-5）— 1分钟

**讲什么**：监控怎么搭的，故障怎么注入的

```
Prometheus 采集 SockShop 9 个服务的指标，全部 UP。
Grafana 自定义仪表盘展示 JVM 内存、GC 频率和运行时长。
ChaosMesh 部署在 K8s 上，6 个组件全部健康。
我们对 carts 和 catalogue 服务注入了 10 个故障实验：
  CPU 90% 压力、网络 1000ms 延迟、Pod 杀除。
验证结论：故障注入全部成功，CPU 压力下吞吐量下降 15%。
```

**PPT页**: 幻灯片4-5（监控面板 + 故障实验表）

---

## 成员3：Selenium 测试（幻灯片6）— 1分钟

**讲什么**：Selenium 怎么测的，结果是什么

```
Selenium 用 headless Chrome 模拟真实用户操作。
测试了 5 个页面：首页、登录、注册、商品目录、购物车。
全部 5/5 PASS。
交互测试：找到 55 个导航链接，成功点击添加购物车按钮。
页面加载时间：登录页 92ms，商品目录 33ms。
模拟了完整的用户购物流程。
```

**PPT页**: 幻灯片6（Selenium 结果表）

---

## 成员4：JMeter 测试（幻灯片7）— 1分钟

**讲什么**：JMeter 怎么测的，性能数据

```
JMeter 5.6.3，测试计划包含线程组和 HTTP 采样器。
对 SockShop 首页发起并发请求：
  成功率 100%，吞吐量 26 req/s，平均响应 17ms。
Windows 下 JMeter 多线程有端口限制，
我们同时用 Python 脚本做了 100 并发验证——100% 成功。
还对比了正常和故障注入下的性能差异：
  故障时吞吐量从 85 降到 72 req/s。
```

**PPT页**: 幻灯片7（JMeter + Python 性能表）

---

## 成员5：USAD 论文复现（幻灯片8-9）— 1分钟

**讲什么**：USAD 是什么，我们怎么复现的

```
USAD 是 KDD 2020 的论文，用对抗训练自编码器做异常检测。
架构：1 个编码器 + 2 个解码器，两阶段训练。
我们用 SockShop 的 30 维合成监控数据做了实验。
结果：Precision 0.826, Recall 0.950, F1 达到 0.884。
关键创新：一个模型支持多级灵敏度，训练只需 3 秒。
论文称比 OmniAnomaly 快 547 倍，我们验证了这个效率优势。
```

**PPT页**: 幻灯片8-9（USAD 架构 + 结果表）

---

## 成员6：PatternMatcher + 总结（幻灯片10-13）— 1分钟

**讲什么**：PatternMatcher 结果 + 整体收获

```
PatternMatcher 是 ISSRE 2021 的论文，做根因定位。
三步法：KS 检验过滤 90% 正常指标 → 分类 13 种异常模式 → 加权排序。
AC@1=1.00——排名第一的就是真正的根因指标，仅需 0.1 秒。
两个算法互补：USAD 做检测，PatternMatcher 做定位。
总结：我们完成了 SockShop 从部署到监控到测试到论文复现的全流程。
监控 9/9，测试 100%，故障注入 10 个实验，两个算法均验证成功。
谢谢各位老师。
```

**PPT页**: 幻灯片10-13（PatternMatcher 结果 + 总结）

---

## 快速记忆卡

| 成员 | 关键词 | 数字 |
|------|--------|------|
| 1 | 部署架构 | 14服务, Docker Compose + K8s |
| 2 | 监控 + ChaosMesh | 9/9 UP, 10个故障实验 |
| 3 | Selenium | 5/5 PASS, 92ms |
| 4 | JMeter | 100%, 26 req/s, 17ms |
| 5 | USAD | F1=0.884, 训练3s |
| 6 | PatternMatcher + 总结 | AC@1=1.00, 0.1s |
